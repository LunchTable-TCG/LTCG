/**
 * Card ability definitions are stored in the cardDefinitions table.
 *
 * Use the effect library (effectLibrary.ts) to build abilities
 * with buildAbility(), simpleAbility(), EFFECT_TEMPLATES, COSTS,
 * CONDITIONS, and TARGETS.
 *
 * Card data is managed through:
 * - /data/cards/*.json (archetype card definitions)
 * - Admin dashboard (card creation/editing)
 * - Seed scripts (initial data population)
 */

export {};
