/**
 * Logger utility
 *
 * Re-exports the ElizaOS core logger for use throughout the plugin.
 */

import { logger as coreLogger } from "@elizaos/core";

export const logger = coreLogger;
export default logger;
