{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "if [ \"$VERCEL_ENV\" = \"production\" ]; then npx convex deploy --cmd 'cd apps/web && bun run build'; else cd apps/web && bun run build; fi",
  "devCommand": "bun run dev:web",
  "installCommand": "bun install",
  "framework": "nextjs",
  "outputDirectory": "apps/web/.next"
}
